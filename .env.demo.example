# MODE
APP_MODE=demo

#DEMO PASSWORD
DEMO_BASIC_AUTH_PASSWORD=demo

# DATABASE
DATABASE_URL=

# ============================================================
# LLM PROVIDER CONFIG
# ============================================================

# Primary LLM provider (gemini, openai, groq, etc.)
LLM_PROVIDER=gemini

# Primary API key used by the active provider
LLM_API_KEY=

# Gemini-specific model (used when LLM_PROVIDER=gemini)
GEMINI_MODEL=gemini-2.5-flash
